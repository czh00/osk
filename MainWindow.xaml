<Window x:Class="OSK.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OSK v1.0.0" Height="330" Width="1000" 
        Topmost="True" WindowStyle="None" AllowsTransparency="True"
        Background="#EE1A1A1A" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        ResizeMode="NoResize" Name="RootWindow">
    <Window.Resources>
        <Style x:Key="KeyButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{Binding TextColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ClipToBounds" Value="True"/>
            <!-- 統一按鍵間距：左右各 2px（可調整為 3/4/6 以改變間距） -->
            <Setter Property="Margin" Value="2,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="25" Height="25" Cursor="SizeNWSE" MouseLeftButtonDown="Resize_Init" MouseMove="Resize_Move" MouseLeftButtonUp="Resize_End" Panel.ZIndex="100" Background="Transparent">
            <Path Data="M0,25 L25,25 L25,0 Z" Fill="#444"/>
            <Path Data="M10,25 L25,10 M15,25 L25,15 M20,25 L25,20" Stroke="#AAA" StrokeThickness="1"/>
        </Grid>
        <Grid Margin="1,1,1,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                <TextBlock Text="透明度: " Foreground="White" VerticalAlignment="Center"/>
                <Slider x:Name="OpacitySlider" IsMoveToPointEnabled="True" Value="{Binding Opacity, ElementName=RootWindow}" Minimum="0.2" Maximum="1.0" Width="80" VerticalAlignment="Center" Focusable="true" Margin="5,0,10,0"/>
                <Button Content=" 🗕 " Click="Minimize_Click" Width="30" Height="25" Margin="0,0,2,0" Background="#333" FontWeight="Bold"/>
                <Button Content=" ✕ " Click="Exit_Click" Width="30" Height="25" Margin="2,0,0,0" Background="#993333"/>
            </StackPanel>
            <Viewbox Grid.Row="10" Stretch="Uniform" VerticalAlignment="Top">
                <ItemsControl Name="KeyBoardItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <!-- 使用 StackPanel（移除 Spacing 以相容所有 WPF 版本） -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource KeyButtonStyle}"
                                                Background="{Binding Background}"
                                                Width="{Binding Width}"
                                                Height="55"
                                                Command="{Binding DataContext.KeyCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}">
                                            <Viewbox Stretch="Uniform" StretchDirection="Both">
                                                <TextBlock Text="{Binding DisplayName}" 
                                                           TextAlignment="Center" 
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center"
                                                           Foreground="{Binding TextColor}" />
                                            </Viewbox>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Viewbox>
        </Grid>
    </Grid>
</Window>